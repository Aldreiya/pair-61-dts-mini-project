import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#7ec8e3',
    },
    secondary: {
      main: '#808080',
    },
    background: {
      paper: '#141414'
    },
    text: {
      primary: '#141414',
      secondary: '#fff',
    },
    
  input: {
    color: "#2EFF22"
  }
  },
});

export default theme;
